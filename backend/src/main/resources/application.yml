server:
  port: ${PORT:8080}
  servlet:
    context-path: ${SPRING_SERVER_CONTEXT_PATH}
  allowed-origins: ${SPRING_SERVER_ALLOWED_ORIGINS}

spring:
  application:
    name: ${SPRING_APPLICATION_NAME}
  profiles:
    active: ${SPRING_PROFILES_ACTIVE}
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: ${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
    properties:
      hibernate:
        format_sql: ${SPRING_JPA_HIBERNATE_FORMAT_SQL:true}

app:
  jwt:
    secret: ${APP_JWT_SECRET}
    expiration: 3600000 # 1 hour

logging:
  level:
    root: ${LOGGING_LEVEL_ROOT:INFO}
    com.reddy.taskmanager: ${LOGGING_LEVEL_TASK_MANAGER:DEBUG}
    org:
      springframework: ${LOGGING_LEVEL_SPRINGFRAMEWORK:INFO}
      hibernate:
        orm:
          connections:
            pooling: ${LOGGING_LEVEL_HIBERNATE_POOLING:WARN}

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always